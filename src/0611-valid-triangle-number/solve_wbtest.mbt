test "example 1" {
  assert_eq!(triangle_number([2, 2, 3, 4]), 3)
}

test "example 2" {
  assert_eq!(triangle_number([4, 2, 3, 4]), 4)
}

test "example 3" {
  assert_eq!(triangle_number([1, 1, 1, 1]), 0)
}
