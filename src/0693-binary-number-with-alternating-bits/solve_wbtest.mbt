test "example 1" {
  assert_eq!(has_alternating_bits(5), true)
}

test "example 2" {
  assert_eq!(has_alternating_bits(7), false)
}

test "example 3" {
  assert_eq!(has_alternating_bits(11), false)
}

test "example 4" {
  assert_eq!(has_alternating_bits(10), true)
}

test "example 5" {
  assert_eq!(has_alternating_bits(1), true)
}
