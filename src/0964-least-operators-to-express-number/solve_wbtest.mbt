test "example 1" {
  assert_eq!(least_ops_express_target(3, 19), 5) // Example test case
}

test "example 2" {
  assert_eq!(least_ops_express_target(5, 501), 8) // Another test case
}

test "example 3" {
  assert_eq!(least_ops_express_target(100, 100000000), 3) // Another test case
}
