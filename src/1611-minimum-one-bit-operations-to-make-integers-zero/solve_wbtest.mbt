test "example 1" {
  assert_eq!(minimum_one_bit_operations(0), 0)
}

test "example 2" {
  assert_eq!(minimum_one_bit_operations(3), 2)
}

test "example 3" {
  assert_eq!(minimum_one_bit_operations(6), 4)
}
