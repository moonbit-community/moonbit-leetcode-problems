test "example 1" {
  assert_eq!(min_boxes_touching_floor(3), 3)
}

test "example 2" {
  assert_eq!(min_boxes_touching_floor(4), 3)
}

test "example 3" {
  assert_eq!(min_boxes_touching_floor(10), 6)
}

test "example 4" {
  assert_eq!(min_boxes_touching_floor(1), 1)
}

test "example 5" {
  assert_eq!(min_boxes_touching_floor(2), 2)
}
